<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues></ManuallySuppressedIssues>
  <CurrentIssues>
    <ID>ComplexCondition:VoiceRecorder.kt$VoiceRecorder$!ok || out == null || !out.exists() || out.length() == 0L</ID>
    <ID>ForbiddenComment:TimelineFragment.kt$TimelineFragment$ S11 message menu, opened with CENTER on a message row. Both this menu and * the edit prompt it can open are plain Dialogs over the still-alive Fragment * view (S9 stays the visible screen underneath), so nothing restores focus to * compose_input on dismiss unless we do it here — one general hook on each * dialog, not per-branch logic (Phase 9, UI improvement plan; also correctly * covers the still-TODO MSG_REPLY branch once it lands, since it'll open a * dialog off this same menu). Navigation-based destinations (toImageViewer, * toMessageInfo, toRoomInfo) are untouched — those already restore focus * correctly via Fragment view recreation. </ID>
    <ID>LongParameterList:TimelineAdapter.kt$TimelineAdapter$( private val onMessageFocused: (EventId) -&gt; Unit, private val onFixEncryption: (EventId) -&gt; Unit, private val onMessageActivated: (TimelineRow.Message) -&gt; Unit, private val onImageBind: (EventId, ImageView) -&gt; Unit, private val onImageActivated: (EventId) -&gt; Unit, private val onAttachmentActivated: (TimelineRow.Attachment) -&gt; Unit, /** Binds a sender avatar (Avatars round) — null clears to the placeholder. */ private val onAvatarBind: (String?, ImageView) -&gt; Unit, /** Populates the "seen by" row with one small avatar per entry. */ private val onSeenByBind: (List&lt;SeenBy&gt;, LinearLayout) -&gt; Unit, /** Populates the reaction-chip row (display-only — see item_message.xml's * header comment on why chips aren't individually tappable). */ private val onReactionsBind: (List&lt;ReactionSummary&gt;, LinearLayout) -&gt; Unit, )</ID>
    <ID>UnusedPrivateProperty:TimelineViewModel.kt$TimelineViewModel$private val clock: MillisClock</ID>
  </CurrentIssues>
</SmellBaseline>
