<?xml version="1.0" encoding="utf-8"?>
<!-- S23 Policy, read-only. Nothing is focusable; softkeys are blank | blank | Back. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/content_pad">

        <TextView
            android:id="@+id/policy_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="?attr/textSizeHeading"
            android:textColor="?attr/colorTextPrimary"
            tools:text="Not managed" />

        <TextView
            android:id="@+id/policy_state_sub"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="?attr/textSizeMeta"
            android:textColor="?attr/colorTextSecondary"
            tools:text="This phone is not managed by an organization." />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/policy_homeserver"
            android:textSize="?attr/textSizeMeta"
            android:textColor="?attr/colorTextSecondary" />

        <TextView
            android:id="@+id/policy_homeserver"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="?attr/textSizeBody"
            android:textColor="?attr/colorTextPrimary"
            tools:text="chats.carpathianserver.org" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/policy_allowed"
            android:textSize="?attr/textSizeMeta"
            android:textColor="?attr/colorTextSecondary" />

        <TextView
            android:id="@+id/policy_allowed"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="?attr/textSizeBody"
            android:textColor="?attr/colorTextPrimary"
            tools:text="All servers allowed" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/policy_direct"
            android:textSize="?attr/textSizeMeta"
            android:textColor="?attr/colorTextSecondary" />

        <TextView
            android:id="@+id/policy_direct"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="?attr/textSizeBody"
            android:textColor="?attr/colorTextPrimary"
            tools:text="Allowed" />
    </LinearLayout>
</ScrollView>
