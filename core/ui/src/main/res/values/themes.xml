<?xml version="1.0" encoding="utf-8"?>
<!-- Base theme for MatChat. AppCompat, not Material: Views deliberately
     (docs/adr/0002). No ripples, no elevation, no animation over 150 ms.

     Theme.MatChat.Base.{Light,Dark} carry every role attr (attrs.xml) that
     does NOT depend on the user's chosen accent or size. Accent is layered
     on top at runtime via Resources.Theme.applyStyle(resId, force = true)
     (MainActivity.onCreate()) — the same "ThemeOverlay" pattern Android
     itself uses — rather than as part of a combined leaf: Theme.MatChat.
     Accent.<Name> below are 16 standalone (parent="") overlays, one per
     Settings > Theme > Accent color choice, each carrying only the roles
     that vary by accent: colorFocusAccent (the trailing-edge focus bar and
     the chrome bars), colorAccent (AppCompat's own tint), and
     colorSurfaceFocused + colorBubbleOwn (both a translucent wash of the
     SAME accent, colors.xml's accent_<name>_tint — every "light shade of
     the accent" role in the app must be one of these two attrs, not a
     fixed hex, or it silently stops tracking the user's theme choice; that
     was a real bug here twice, on both of these, before this fix). Kept
     flat (16) rather than combined with the base (which would be 32, and
     64 once text size — also layered this way, see Theme.MatChat.Size.*
     below — is folded in) so this list doesn't grow with the number of
     bases or sizes. Theme.MatChat itself is the manifest/default fallback
     for any context built before the stored preference is read
     (MainActivity always calls setTheme() + applyStyle() with the resolved
     layers first, so this is a safety net, not the normal path) — fully
     baked (Base.Light + Green), not a bare alias to an overlay, since an
     overlay's parent="" means it resolves nothing on its own. -->
<resources>
    <style name="Theme.MatChat.Base.Light" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorSurface">@color/palette_paper</item>
        <item name="colorSurfaceBright">@color/palette_paper_bright</item>
        <item name="colorSurfaceInverse">@color/palette_ink</item>
        <item name="colorTextPrimary">@color/palette_ink</item>
        <item name="colorTextSecondary">@color/palette_stone</item>
        <item name="colorTextOnInverse">@color/text_on_inverse</item>
        <item name="colorTextOnFocus">@color/text_on_focus</item>
        <item name="colorTextMetaOnFocus">@color/text_meta_on_focus</item>
        <item name="colorTextLink">@color/palette_amber</item>
        <item name="colorDivider">@color/palette_line</item>
        <item name="colorEncrypted">@color/palette_green</item>
        <item name="colorWarning">@color/warning</item>
        <item name="colorError">@color/error</item>
        <item name="colorScrim">@color/scrim</item>
        <item name="colorBubbleReceived">@color/palette_bubble_received_light</item>

        <item name="android:windowBackground">?attr/colorSurface</item>
        <item name="android:textColorPrimary">?attr/colorTextPrimary</item>
        <item name="android:textColorSecondary">?attr/colorTextSecondary</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!-- These are D-pad devices: never behave as if a touch is available. -->
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- Mirrors Base.Light role for role: a near-black surface, a lighter
         "raised panel" tone for chrome/dialogs, and brighter green/amber so
         the encrypted indicator and the link text keep their contrast
         against a dark surface (colors.xml). -->
    <style name="Theme.MatChat.Base.Dark" parent="Theme.AppCompat.NoActionBar">
        <item name="colorSurface">@color/palette_night</item>
        <item name="colorSurfaceBright">@color/palette_night_bright</item>
        <item name="colorSurfaceInverse">@color/palette_night_bright</item>
        <item name="colorTextPrimary">@color/palette_paper_bright</item>
        <item name="colorTextSecondary">@color/palette_mist_dim</item>
        <item name="colorTextOnInverse">@color/text_on_inverse</item>
        <item name="colorTextOnFocus">@color/text_on_focus</item>
        <item name="colorTextMetaOnFocus">@color/text_meta_on_focus</item>
        <item name="colorTextLink">@color/palette_amber_bright</item>
        <item name="colorDivider">@color/palette_line_dark</item>
        <item name="colorEncrypted">@color/palette_green_bright</item>
        <item name="colorWarning">@color/warning</item>
        <item name="colorError">@color/error</item>
        <item name="colorScrim">@color/scrim</item>
        <item name="colorBubbleReceived">@color/palette_night_bright</item>

        <item name="android:windowBackground">?attr/colorSurface</item>
        <item name="android:textColorPrimary">?attr/colorTextPrimary</item>
        <item name="android:textColorSecondary">?attr/colorTextSecondary</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- Accent overlays (parent="" — standalone, not inheriting any base;
         layered onto whichever base is already set via applyStyle(), see
         the file header). Each carries the same 4 roles the old combined
         leaves did, driven by one accent. -->
    <style name="Theme.MatChat.Accent.Green" parent="">
        <item name="colorFocusAccent">@color/accent_green</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_green_tint</item>
        <item name="colorBubbleOwn">@color/accent_green_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Amber" parent="">
        <item name="colorFocusAccent">@color/accent_amber</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_amber_tint</item>
        <item name="colorBubbleOwn">@color/accent_amber_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Blue" parent="">
        <item name="colorFocusAccent">@color/accent_blue</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_blue_tint</item>
        <item name="colorBubbleOwn">@color/accent_blue_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Plum" parent="">
        <item name="colorFocusAccent">@color/accent_plum</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_plum_tint</item>
        <item name="colorBubbleOwn">@color/accent_plum_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Teal" parent="">
        <item name="colorFocusAccent">@color/accent_teal</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_teal_tint</item>
        <item name="colorBubbleOwn">@color/accent_teal_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Cyan" parent="">
        <item name="colorFocusAccent">@color/accent_cyan</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_cyan_tint</item>
        <item name="colorBubbleOwn">@color/accent_cyan_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Indigo" parent="">
        <item name="colorFocusAccent">@color/accent_indigo</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_indigo_tint</item>
        <item name="colorBubbleOwn">@color/accent_indigo_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Violet" parent="">
        <item name="colorFocusAccent">@color/accent_violet</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_violet_tint</item>
        <item name="colorBubbleOwn">@color/accent_violet_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Orchid" parent="">
        <item name="colorFocusAccent">@color/accent_orchid</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_orchid_tint</item>
        <item name="colorBubbleOwn">@color/accent_orchid_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Rose" parent="">
        <item name="colorFocusAccent">@color/accent_rose</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_rose_tint</item>
        <item name="colorBubbleOwn">@color/accent_rose_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Rust" parent="">
        <item name="colorFocusAccent">@color/accent_rust</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_rust_tint</item>
        <item name="colorBubbleOwn">@color/accent_rust_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Ochre" parent="">
        <item name="colorFocusAccent">@color/accent_ochre</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_ochre_tint</item>
        <item name="colorBubbleOwn">@color/accent_ochre_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Olive" parent="">
        <item name="colorFocusAccent">@color/accent_olive</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_olive_tint</item>
        <item name="colorBubbleOwn">@color/accent_olive_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Forest" parent="">
        <item name="colorFocusAccent">@color/accent_forest</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_forest_tint</item>
        <item name="colorBubbleOwn">@color/accent_forest_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Slate" parent="">
        <item name="colorFocusAccent">@color/accent_slate</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_slate_tint</item>
        <item name="colorBubbleOwn">@color/accent_slate_tint</item>
    </style>
    <style name="Theme.MatChat.Accent.Wine" parent="">
        <item name="colorFocusAccent">@color/accent_wine</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_wine_tint</item>
        <item name="colorBubbleOwn">@color/accent_wine_tint</item>
    </style>

    <!-- Size overlays (parent="" — same standalone shape as the accent
         overlays above; layered on top of those via a second applyStyle()
         call, MainActivity.onCreate()). Settings > Text size (UX-SPEC §S16)
         switches between these three — every dimension role that scales
         with it lives here, nowhere else. Normal is the default set (the
         `_large` dimens — this app's whole point is legibility on a small
         screen, so "normal" should already read comfortably); Small is the
         reduced option, for anyone who wants more on screen at once; Large
         is a further step up (the `_xlarge` dimens) for anyone who wants it
         bigger still. -->
    <style name="Theme.MatChat.Size.Normal" parent="">
        <item name="textSizeBody">@dimen/text_body_large</item>
        <item name="textSizeLabel">@dimen/text_label_large</item>
        <item name="textSizeMeta">@dimen/text_meta_large</item>
        <item name="textSizeRoomName">@dimen/text_room_name_large</item>
        <item name="textSizeRoomPreview">@dimen/text_room_preview_large</item>
        <item name="textSizeAddress">@dimen/text_address_large</item>
        <item name="textSizeSender">@dimen/text_sender_large</item>
        <item name="textSizeHeading">@dimen/text_heading_large</item>
        <item name="avatarSizeList">@dimen/avatar_size_list_large</item>
        <item name="avatarSizeSender">@dimen/avatar_size_sender_large</item>
        <item name="avatarSizeSeenBy">@dimen/avatar_size_seen_by_large</item>
        <item name="rowMinHeight">@dimen/row_min_height_large</item>
        <item name="rowMinHeightCompact">@dimen/row_min_height_compact_large</item>
    </style>
    <style name="Theme.MatChat.Size.Small" parent="">
        <item name="textSizeBody">@dimen/text_body</item>
        <item name="textSizeLabel">@dimen/text_label</item>
        <item name="textSizeMeta">@dimen/text_meta</item>
        <item name="textSizeRoomName">@dimen/text_room_name</item>
        <item name="textSizeRoomPreview">@dimen/text_room_preview</item>
        <item name="textSizeAddress">@dimen/text_address</item>
        <item name="textSizeSender">@dimen/text_sender</item>
        <item name="textSizeHeading">@dimen/text_heading</item>
        <item name="avatarSizeList">@dimen/avatar_size_list</item>
        <item name="avatarSizeSender">@dimen/avatar_size_sender</item>
        <item name="avatarSizeSeenBy">@dimen/avatar_size_seen_by</item>
        <item name="rowMinHeight">@dimen/row_min_height</item>
        <item name="rowMinHeightCompact">@dimen/row_min_height_compact</item>
    </style>
    <style name="Theme.MatChat.Size.Large" parent="">
        <item name="textSizeBody">@dimen/text_body_xlarge</item>
        <item name="textSizeLabel">@dimen/text_label_xlarge</item>
        <item name="textSizeMeta">@dimen/text_meta_xlarge</item>
        <item name="textSizeRoomName">@dimen/text_room_name_xlarge</item>
        <item name="textSizeRoomPreview">@dimen/text_room_preview_xlarge</item>
        <item name="textSizeAddress">@dimen/text_address_xlarge</item>
        <item name="textSizeSender">@dimen/text_sender_xlarge</item>
        <item name="textSizeHeading">@dimen/text_heading_xlarge</item>
        <item name="avatarSizeList">@dimen/avatar_size_list_xlarge</item>
        <item name="avatarSizeSender">@dimen/avatar_size_sender_xlarge</item>
        <item name="avatarSizeSeenBy">@dimen/avatar_size_seen_by_xlarge</item>
        <item name="rowMinHeight">@dimen/row_min_height_xlarge</item>
        <item name="rowMinHeightCompact">@dimen/row_min_height_compact_xlarge</item>
    </style>

    <!-- Manifest default / fallback — see the file header. Baked with the
         Normal (large) size set, matching the default preference. -->
    <style name="Theme.MatChat" parent="Theme.MatChat.Base.Light">
        <item name="colorFocusAccent">@color/accent_green</item>
        <item name="colorAccent">?attr/colorFocusAccent</item>
        <item name="colorSurfaceFocused">@color/accent_green_tint</item>
        <item name="colorBubbleOwn">@color/accent_green_tint</item>
        <item name="textSizeBody">@dimen/text_body_large</item>
        <item name="textSizeLabel">@dimen/text_label_large</item>
        <item name="textSizeMeta">@dimen/text_meta_large</item>
        <item name="textSizeRoomName">@dimen/text_room_name_large</item>
        <item name="textSizeRoomPreview">@dimen/text_room_preview_large</item>
        <item name="textSizeAddress">@dimen/text_address_large</item>
        <item name="textSizeSender">@dimen/text_sender_large</item>
        <item name="textSizeHeading">@dimen/text_heading_large</item>
        <item name="avatarSizeList">@dimen/avatar_size_list_large</item>
        <item name="avatarSizeSender">@dimen/avatar_size_sender_large</item>
        <item name="avatarSizeSeenBy">@dimen/avatar_size_seen_by_large</item>
        <item name="rowMinHeight">@dimen/row_min_height_large</item>
        <item name="rowMinHeightCompact">@dimen/row_min_height_compact_large</item>
    </style>

    <!-- A focusable row: full-width inverse focus block, no touch feedback.
         parent="" so the dotted name is not read as inheriting from a
         non-existent "Widget.MatChat" (AAPT implicit-parent rule). -->
    <style name="Widget.MatChat.Row" parent="">
        <item name="android:background">@drawable/focus_selector</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">false</item>
        <item name="android:minHeight">?attr/rowMinHeight</item>
        <item name="android:paddingStart">@dimen/content_pad</item>
        <item name="android:paddingEnd">@dimen/content_pad</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- Text.* use parent="" for the same implicit-parent reason as above. -->
    <style name="Text.Body" parent="">
        <item name="android:textSize">?attr/textSizeBody</item>
        <item name="android:textColor">?attr/colorTextOnFocus</item>
    </style>

    <style name="Text.Label" parent="">
        <item name="android:textSize">?attr/textSizeLabel</item>
        <item name="android:textColor">?attr/colorTextOnFocus</item>
    </style>

    <style name="Text.Meta" parent="">
        <item name="android:textSize">?attr/textSizeMeta</item>
        <item name="android:textColor">?attr/colorTextMetaOnFocus</item>
    </style>

    <!-- The MenuSheet dialog: bottom-anchored, non-floating, BACK dismisses. Uses
         a plain (non-AppCompat) dialog theme so a non-floating window is allowed. -->
    <style name="Theme.MatChat.Menu" parent="@android:style/Theme.DeviceDefault.Light.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
